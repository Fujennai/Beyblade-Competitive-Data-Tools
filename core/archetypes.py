# Archivo eliminado: clustering KMeans nunca se llegó a usar desde la app.
# El cálculo de arquetipos se hace en scraper.py (umbrales por Pts/Combate).
# Puedes borrar este archivo con `git rm core/archetypes.py`.
